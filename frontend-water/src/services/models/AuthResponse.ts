export interface LoginResponse {
  accessToken: string
  tokenType: string
  userId: number
  name: string
}

export interface RegisterResponse {
  accessToken: string
  tokenType: string
  userId: number
  name: string
}