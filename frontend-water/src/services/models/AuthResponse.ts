export interface LoginResponse {
  access_token: string
  token_type: string
  user_id: number
  name: string
}

export interface RegisterResponse {
  access_token: string
  token_type: string
  user_id: number
  name: string
}